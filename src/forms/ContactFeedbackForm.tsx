import React from 'react'

type PropsType = {

}

export const ContactFeedbackForm: React.FC<PropsType> = (props) => {

    const { } = props

    return (
        <form>
            Форма
        </form>
    )
}